# Comando de remodelación con el XGB actualizado

Ejecuta desde la raíz del repo:

```bash
PYTHONPATH=. python3 -m optimization.remodel.run_opt --pid 527105030 --budget 80000
```

Sustituye el PID y el presupuesto si quieres probar otra casa o escenario.
